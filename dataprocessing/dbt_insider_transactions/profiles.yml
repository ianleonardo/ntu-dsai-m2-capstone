dbt_insider_transactions:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GOOGLE_PROJECT_ID', 'ntu-dsai-488112') }}"
      dataset: "{{ env_var('BIGQUERY_DATASET', 'insider_transactions') }}"
      threads: 1
      timeout_seconds: 300
      location: asia-southeast1
      priority: interactive
