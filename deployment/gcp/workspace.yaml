# Points the Dagster web UI at the gRPC code server (user code).
# K8s: Service name `dagster-user-code` resolves inside the cluster.
load_from:
  - grpc_server:
      host: dagster-user-code
      port: 4000
      location_name: user_code
