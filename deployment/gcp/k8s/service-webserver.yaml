apiVersion: v1
kind: Service
metadata:
  name: dagster-webserver
  namespace: dagster
spec:
  selector:
    app: dagster-webserver
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: LoadBalancer
